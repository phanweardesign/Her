const HerStoryIntelligence={parseReply(r){if(r&&typeof r==='object')return r;try{return JSON.parse(String(r||'').trim())}catch(e){throw new Error("Her returned invalid character suggestions.")}},async scanCharacters({book,chapter,manuscript}){const reply=await HerAI.scanCharacters(manuscript,{book:{id:book.id,title:book.title,genre:book.genre},currentChapter:{id:chapter.id,title:chapter.title},existingCharacters:(book.characters||[]).map(c=>({id:c.id,name:c.name,nickname:c.nickname,role:c.role}))});const x=this.parseReply(reply);return {characters:Array.isArray(x.characters)?x.characters.filter(v=>v?.name).map(v=>({name:String(v.name).trim(),role:String(v.role||'Supporting Character').trim(),age:String(v.age||'').trim(),occupation:String(v.occupation||'').trim(),relationships:Array.isArray(v.relationships)?v.relationships.map(String):[],firstAppearance:String(v.firstAppearance||chapter.title||'').trim(),confidence:Math.max(0,Math.min(1,Number(v.confidence||0))),evidence:String(v.evidence||'').trim()})):[]}},acceptCharacter(bookId,chapterId,s){return HerCharacters.create(bookId,{...s,status:'Draft',source:'story-intelligence',linkedChapters:[chapterId]})}};
